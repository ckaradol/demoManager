import 'dart:ui';

class AppTranslate{
  static const String assetLocations="assets/translations";
  static const List<Locale> supportedLocales=[Locale('en'), Locale('tr')];
  static const Locale fallBackLocale=Locale('en');
}